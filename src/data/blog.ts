import type { BlogPost } from "@/types";

// No articles have been published yet. Add entries here when ready —
// the listing, detail, and related-articles UI already handle this data shape.
export const posts: BlogPost[] = [];
